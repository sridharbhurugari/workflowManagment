import { StatusTransperPipePipe } from './status-transper-pipe.pipe';

describe('StatusTransperPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTransperPipePipe();
    expect(pipe).toBeTruthy();
  });
});
